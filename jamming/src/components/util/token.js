var client_id = 'CLIENT_ID';
var redirect_uri = 'http://localhost:3000/callback';

console.log();

const Spotify = {
    getAccessToken() {
        if (accessToken) {
            return accessToken;
        }
    }
}

export { Spotify };
